apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80         # Service port (internal/external access)
      targetPort: 5000 # Container port
  type: LoadBalancer  # Use NodePort if you don’t have a cloud LB
